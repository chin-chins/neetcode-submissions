class Solution:
    def hasDuplicate(self, nums: List[int]) -> bool:
        dups = dict()
        for x in nums:
            dups.add()
            if dups.size() < nums.size():
                return true

        return false 
        
        